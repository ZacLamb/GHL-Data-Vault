{
  "name": "ghl-vault",
  "version": "0.1.0",
  "description": "Exports every file stored in GoHighLevel sub-accounts (custom field uploads, conversation attachments, recordings, form/survey uploads, media library, documents & contracts) to Cloudflare R2 with a joinable manifest.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.650.0",
    "@aws-sdk/lib-storage": "^3.650.0",
    "archiver": "^7.0.1",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "@aws-sdk/s3-request-presigner": "^3.650.0"
  }
}